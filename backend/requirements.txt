setuptools<81  # pyworld needs pkg_resources, removed in setuptools>=81
numpy>=1.23.5,<2
scipy
librosa==0.10.2
soundfile
sounddevice
pedalboard
faiss-cpu
praat-parselmouth
pyworld
torchcrepe
ffmpeg-python
fairseq==0.12.2
websockets
tqdm

# DDSP-SVC (rectified-flow) real-time inference -- runs in-process here
# alongside RVC, same as rvc.engine/rvc.streaming above.
PyYAML
transformers
resampy
matplotlib  # nsf_hifigan/utils.py imports this unconditionally even though
            # it's only used by a training-time plotting helper never called
            # during inference
